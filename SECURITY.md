# 安全策略

## 支持范围

只保证最新一次发布的版本。本项目是一个**提示词与人设引擎**，随宿主（DeepSeek Harness / ZCode）版本演进。

## 报告方式

优先用 GitHub 的 **Private vulnerability reporting**（仓库页 → Security → Report a vulnerability）。
请附：宿主版本、本插件版本、复现步骤、影响描述。**不要在 issue 里贴任何凭据或你的私人配置内容。**

## 安全边界（设计承诺）

- **不联网**：全库没有对外 HTTP 调用，也没有 `child_process`。
- **只碰你自己的文件**：`$DSH_HOME/whale-suite/config.json`、同目录的 `session-flags.json`、记忆收件箱、人设预设目录。
- **失败即降级**：任何异常都返回空段——最坏结果是「没有人设」，永远不允许让会话发不出话。
- **插值关闭**：提示词段不做 `{{变量}}` 全量插值（未知变量会让宿主在装配期抛错），`{{cwd}}` 由本插件自行替换。

## 已知残余风险

1. **长期记忆的写入闸门是代码强制的**（AI 只能写候选，人工确认后才注入），但**确认动作本身**依赖你与模型的对话——
   恶意提示词理论上可以诱导出一次假确认。缓解：收件箱条目以「数据、非指令」呈现、条目内换行折叠、条数上限，
   外加你定期翻看收件箱文件（删行安全，坏行自动跳过）。
2. **人设是概率性的**：它是行为先验，不是权限系统。需要硬保证的事请用宿主的权限与沙箱配置。
3. **记忆会跨会话生效**：这是设计目的，也意味着一次错误确认会长期存在——所以保留「删行即失效」这条最简单的手动通道。
